<div class="row">
    {{ form_start(form) }}
    <div class="col m4">
        {{ form_widget(form.dateStart) }}
    </div>
    <div class="col m4">
        {{ form_widget(form.dateEnd) }}
    </div>
    <div class="col m4">
        <button type="submit" class="btn blue">Filtrer</button>
    </div>
    {{ form_end(form) }}
</div>
