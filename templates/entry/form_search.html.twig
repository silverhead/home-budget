{{ form_start(form) }}
    <div class="row m-3">
        <div class="w-3-12 p-1">
            {{ form_widget(form.dateStart) }}
        </div>
        <div class="w-3-12 p-1">
            {{ form_widget(form.dateEnd) }}
        </div>
        <div class="w-2-12 p-1">
            <button type="submit" class="btn btn-secondary">{{ 'entry.list.filter_button'|trans }}</button>
        </div>
    </div>
{{ form_end(form) }}
