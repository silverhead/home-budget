{{ form_start(form) }}
    <div class="row m-3">
        <div class="col-auto">
            {{ form_widget(form.dateStart) }}
        </div>
        <div class="col-auto">
            {{ form_widget(form.dateEnd) }}
        </div>
        <div class="col-auto">
            <button type="submit" class="btn btn-outline-info">{{ 'entry.list.filter_button'|trans }}</button>
        </div>
    </div>
{{ form_end(form) }}
